Chạy dự án
--BE: npm run dev

--server: node server.js

--FE:npm run dev

--Run: docker

--ngrok: ngrok http 3008(take port) 
change port of sepay

TablePlus: POSBE-local
MYSQL:docker exec -it mysql-container mysql -u root -p
      Enter password:pass123
      use POSBE
